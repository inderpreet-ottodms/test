import { RemoveDashDirective } from './remove-dash.directive';

describe('RemoveDashDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveDashDirective();
    expect(directive).toBeTruthy();
  });
});
